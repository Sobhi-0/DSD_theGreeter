import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([

    // Paths defined for every part of the front-end
    {
        path: "/",
        element: <WhateverPage></WhateverPage>
    }

]);
